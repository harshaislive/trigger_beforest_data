{
  "name": "crewai-manychat-convex",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "devDependencies": {
    "convex": "^1.32.0"
  }
}
